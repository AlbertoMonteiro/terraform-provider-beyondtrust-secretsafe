name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

env:
  PROVIDER_NAME: secretsafe
  DOTNET_VERSION: '10.0'

jobs:
  build:
    name: Build ${{ matrix.os }}_${{ matrix.arch }}
    runs-on: ${{ matrix.runner }}
    continue-on-error: true
    strategy:
      matrix:
        include:
          # Linux (Alpine Docker / musl — StaticExecutable=false to preserve dlopen)
          - { os: linux,   arch: amd64, rid: linux-musl-x64,   platform: linux/amd64,  runner: ubuntu-latest,    use_qemu: false }
          # Windows
          - { os: windows, arch: amd64, rid: win-x64,          runner: windows-latest, use_qemu: false }

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ── Linux ──────────────────────────────────────────────────────────────

      - name: Set up QEMU (cross-build)
        if: matrix.use_qemu
        uses: docker/setup-qemu-action@v3

      - name: Build via Alpine Docker (Native AOT / musl)
        if: matrix.os == 'linux'
        run: |
          docker run --rm \
            --platform ${{ matrix.platform }} \
            -v "${{ github.workspace }}:/src" \
            -w /src \
            mcr.microsoft.com/dotnet/sdk:10.0-alpine \
            sh -c "
              apk add --no-cache clang gcc build-base zlib-dev musl-dev gcompat && \
              dotnet publish BeyondTrust.SecretSafeProvider/BeyondTrust.SecretSafeProvider.csproj \
                -c Release \
                -r ${{ matrix.rid }} \
                -o /src/dist/${{ matrix.os }}_${{ matrix.arch }} \
                -p:PublishAot=true \
                -p:StaticExecutable=false \
                --self-contained true
            "

      # ── macOS / Windows ────────────────────────────────────────────────────

      - name: Setup .NET SDK
        if: matrix.os != 'linux'
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Build (macOS / Windows Native AOT)
        if: matrix.os != 'linux'
        shell: bash
        run: |
          dotnet publish BeyondTrust.SecretSafeProvider/BeyondTrust.SecretSafeProvider.csproj \
            -c Release \
            -r ${{ matrix.rid }} \
            -o dist/${{ matrix.os }}_${{ matrix.arch }} \
            -p:PublishAot=true \
            --self-contained true

      # ── Package ────────────────────────────────────────────────────────────

      - name: Package binary into zip (Linux / macOS)
        if: matrix.os != 'windows'
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          BINARY="terraform-provider-${PROVIDER_NAME}"
          BINARY_VERSIONED="terraform-provider-${PROVIDER_NAME}_v${VERSION}"
          ZIPNAME="${BINARY}_${VERSION}_${{ matrix.os }}_${{ matrix.arch }}.zip"

          cp "dist/${{ matrix.os }}_${{ matrix.arch }}/${BINARY}" "${BINARY_VERSIONED}"
          zip "${ZIPNAME}" "${BINARY_VERSIONED}" LICENSE README.md
          rm "${BINARY_VERSIONED}"
          echo "Built: ${ZIPNAME}"

      - name: Package binary into zip (Windows)
        if: matrix.os == 'windows'
        shell: pwsh
        run: |
          $version = $env:GITHUB_REF_NAME -replace '^v', ''
          $binary  = "terraform-provider-$env:PROVIDER_NAME"
          $binaryVersioned = "${binary}_v${version}.exe"
          $zipName = "${binary}_${version}_${{ matrix.os }}_${{ matrix.arch }}.zip"
          $src     = "dist\${{ matrix.os }}_${{ matrix.arch }}\${binary}.exe"

          Copy-Item -Path $src -Destination $binaryVersioned
          Compress-Archive -Path $binaryVersioned, LICENSE, README.md -DestinationPath $zipName
          Remove-Item -Path $binaryVersioned
          Write-Host "Built: $zipName"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: provider-${{ matrix.os }}-${{ matrix.arch }}
          path: "*.zip"
          if-no-files-found: error

  release:
    name: Create GitHub Release
    needs: build
    if: always()
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          merge-multiple: true
          # continue even if no artifacts exist (all builds may have failed)
          pattern: 'provider-*'

      - name: Check for artifacts
        id: check
        run: |
          if ls *.zip 2>/dev/null | grep -q .; then
            echo "has_artifacts=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_artifacts=false" >> "$GITHUB_OUTPUT"
            echo "No zip artifacts found — skipping release."
          fi

      - name: Generate manifest.json
        if: steps.check.outputs.has_artifacts == 'true'
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          MANIFEST_FILE="terraform-provider-${PROVIDER_NAME}_${VERSION}_manifest.json"

          cat > "${MANIFEST_FILE}" << 'MANIFEST_EOF'
          {
            "version": 1,
            "metadata": {
              "protocol_versions": ["5.2"]
            }
          }
          MANIFEST_EOF

          cat "${MANIFEST_FILE}"

      - name: Generate SHA256SUMS
        if: steps.check.outputs.has_artifacts == 'true'
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          SUMS_FILE="terraform-provider-${PROVIDER_NAME}_${VERSION}_SHA256SUMS"
          MANIFEST_FILE="terraform-provider-${PROVIDER_NAME}_${VERSION}_manifest.json"

          sha256sum *.zip "${MANIFEST_FILE}" > "${SUMS_FILE}"
          cat "${SUMS_FILE}"

      - name: Import GPG key
        if: steps.check.outputs.has_artifacts == 'true'
        uses: crazy-max/ghaction-import-gpg@v6
        with:
          gpg_private_key: ${{ secrets.GPG_PRIVATE_KEY }}
          passphrase: ${{ secrets.GPG_PASSPHRASE }}
          git_user_signingkey: true
          git_commit_gpgsign: true
          git_tag_gpgsign: true

      - name: Sign SHA256SUMS
        if: steps.check.outputs.has_artifacts == 'true'
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          SUMS_FILE="terraform-provider-${PROVIDER_NAME}_${VERSION}_SHA256SUMS"
          # Generate detached signature (ASCII-armored format)
          gpg --batch --yes --armor --detach-sign "${SUMS_FILE}"
          # Rename .asc to .sig for Terraform Registry
          mv "${SUMS_FILE}.asc" "${SUMS_FILE}.sig"
          ls -la "${SUMS_FILE}"*

      - name: Create GitHub Release
        if: steps.check.outputs.has_artifacts == 'true'
        uses: softprops/action-gh-release@v2
        with:
          files: |
            *.zip
            *_manifest.json
            *_SHA256SUMS
            *_SHA256SUMS.sig
          generate_release_notes: true
          fail_on_unmatched_files: false
